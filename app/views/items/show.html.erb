<!-- this is what's being displayed when user hits submit -->
<li><%= @item.title %> : <%= @item.description %> size: (<%= @item.size %>) <% if @item.url %> | <%= link_to @item.url, "http://#{@item.url}", target: "_blank" %> <% end %> | <%= link_to "edit", edit_box_item_path(@box, @item) %> | <%= link_to "delete", [@box, @item], :method => :delete %> Note: deleting is final!</li>
<p><%= image_tag @item.photo.url(:thumb) %></p>
