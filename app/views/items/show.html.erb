
  <!-- this is what's being displayed when user hits submit -->
  <li><%= @item.title %>: <%= @item.description %> (size: <%= @item.size %>) <% if @item.url %> | <%= link_to @item.url, "http://#{@item.url}", target: "_blank" %> <% end %> <% if current_user.try(:admin?) %> | <%= link_to "edit", edit_box_item_path(@box, @item) %> | <%= link_to "delete", [@box, @item], :method => :delete %> Note: deleting is final! <% end %>
